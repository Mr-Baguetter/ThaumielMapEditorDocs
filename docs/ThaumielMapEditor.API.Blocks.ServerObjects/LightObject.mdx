---
title: LightObject
sidebar_label: 🔷 LightObject
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Badge from '@site/src/components/Badge';

# 🔷 LightObject

<Badge color="blue">class</Badge> <Badge color="secondary">`ThaumielMapEditor.API.Blocks.ServerObjects`</Badge>

## Overview

### Properties

| Name | Type | Access | Description |
|------|------|:------:|-------------|
| [`Base`](#base) | `LightSourceToy` | `get` | The underlying networked light toy instance created from the prefab. |
| [`InnerSpotAngle`](#innerspotangle) | `float` | `get` `set` | The inner spot angle for spot lights. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present. |
| [`LightColor`](#lightcolor) | `Color` | `get` `set` | The color of the light. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present. |
| [`LightIntensity`](#lightintensity) | `float` | `get` `set` | The intensity of the light. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present. |
| [`LightRange`](#lightrange) | `float` | `get` `set` | The range of the light. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present. |
| [`LightShape`](#lightshape) | `LightShape` | `get` `set` | The legacy light shape setting. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present. |
| [`LightType`](#lighttype) | `LightType` | `get` `set` | The general type of the light (e.g. Point, Spot). Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present. |
| [`ObjectType`](#objecttype) | `ObjectType` | `get` `set` | _No description_ |
| [`ShadowStrength`](#shadowstrength) | `float` | `get` `set` | The strength of shadows produced by the light. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present. |
| [`ShadowType`](#shadowtype) | `LightShadows` | `get` `set` | The shadow type used by the light. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present. |
| [`SpotAngle`](#spotangle) | `float` | `get` `set` | The outer spot angle for spot lights. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present. |

### Methods

| Signature | Returns | Description |
|-----------|---------|-------------|
| [`DeserializeValues(SerializableObject)`](#deserializevalues) | `void` | Reads expected light properties from the provided `ThaumielMapEditor.API.Seriali… |
| [`SpawnObject(SchematicData, SerializableObject)`](#spawnobject) | `void` | _No description_ |

## Properties

### `Base`

**Type:** `LightSourceToy`&emsp;**Access:** `get`

The underlying networked light toy instance created from the prefab.

:::note
May be null until `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.SpawnObject(ThaumielMapEditor.API.Data.SchematicData,ThaumielMapEditor.API.Serialization.SerializableObject)` is called.
:::

---

### `InnerSpotAngle`

**Type:** `float`&emsp;**Access:** `get` `set`

The inner spot angle for spot lights. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present.

---

### `LightColor`

**Type:** `Color`&emsp;**Access:** `get` `set`

The color of the light. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present.

---

### `LightIntensity`

**Type:** `float`&emsp;**Access:** `get` `set`

The intensity of the light. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present.

---

### `LightRange`

**Type:** `float`&emsp;**Access:** `get` `set`

The range of the light. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present.

---

### `LightShape`

**Type:** `LightShape`&emsp;**Access:** `get` `set`

The legacy light shape setting. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present.

---

### `LightType`

**Type:** `LightType`&emsp;**Access:** `get` `set`

The general type of the light (e.g. Point, Spot). Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present.

---

### `ObjectType`

**Type:** `ObjectType`&emsp;**Access:** `get` `set`

---

### `ShadowStrength`

**Type:** `float`&emsp;**Access:** `get` `set`

The strength of shadows produced by the light. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present.

---

### `ShadowType`

**Type:** `LightShadows`&emsp;**Access:** `get` `set`

The shadow type used by the light. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present.

---

### `SpotAngle`

**Type:** `float`&emsp;**Access:** `get` `set`

The outer spot angle for spot lights. Setting this updates the `ThaumielMapEditor.API.Blocks.ServerObjects.LightObject.Base` instance if present.

---

## Methods

### `DeserializeValues`

```csharp title="DeserializeValues"
public void DeserializeValues(SerializableObject serializable)
```

Reads expected light properties from the provided `ThaumielMapEditor.API.Serialization.SerializableObject` and sets local properties.

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `serializable` | `SerializableObject` | Serialized object that should contain light-specific values. |

---

### `SpawnObject`

```csharp title="SpawnObject"
public void SpawnObject(
    SchematicData schematic,
    SerializableObject serializable
)
```

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `schematic` | `SchematicData` |  |
| `serializable` | `SerializableObject` |  |

---

