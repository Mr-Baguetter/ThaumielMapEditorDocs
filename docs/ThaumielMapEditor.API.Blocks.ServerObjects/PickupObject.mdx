---
title: PickupObject
sidebar_label: 🔷 PickupObject
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Badge from '@site/src/components/Badge';

# 🔷 PickupObject

<Badge color="blue">class</Badge> <Badge color="secondary">`ThaumielMapEditor.API.Blocks.ServerObjects`</Badge>

## Overview

### Properties

| Name | Type | Access | Description |
|------|------|:------:|-------------|
| [`IsInfinite`](#isinfinite) | `bool` | `get` | Whether this pickup should be treated as infinite (no depletion). |
| [`ItemToSpawn`](#itemtospawn) | `ItemType` | `get` | The type of item that this pickup will spawn. |
| [`MaxAmount`](#maxamount) | `UInt32` | `get` | Maximum stack/amount that the spawned pickup can contain. |
| [`ObjectType`](#objecttype) | `ObjectType` | `get` `set` | Gets or sets the type of the object represented by this instance. |
| [`SpawnPercentage`](#spawnpercentage) | `float` | `get` | Chance (0-100) that this pickup will actually spawn when processed. |

### Methods

| Signature | Returns | Description |
|-----------|---------|-------------|
| [`ParseValues(SerializableObject)`](#parsevalues) | `void` | Parse pickup-specific values from a `ThaumielMapEditor.API.Serialization.Seriali… |
| [`SpawnObject(SchematicData, SerializableObject)`](#spawnobject) | `void` | Spawns the specified `ThaumielMapEditor.API.Blocks.ServerObject` into the server… |

## Properties

### `IsInfinite`

**Type:** `bool`&emsp;**Access:** `get`

Whether this pickup should be treated as infinite (no depletion).

---

### `ItemToSpawn`

**Type:** `ItemType`&emsp;**Access:** `get`

The type of item that this pickup will spawn.

---

### `MaxAmount`

**Type:** `UInt32`&emsp;**Access:** `get`

Maximum stack/amount that the spawned pickup can contain.

---

### `ObjectType`

**Type:** `ObjectType`&emsp;**Access:** `get` `set`

Gets or sets the type of the object represented by this instance.

---

### `SpawnPercentage`

**Type:** `float`&emsp;**Access:** `get`

Chance (0-100) that this pickup will actually spawn when processed.

---

## Methods

### `ParseValues`

```csharp title="ParseValues"
public void ParseValues(SerializableObject serializable)
```

Parse pickup-specific values from a `ThaumielMapEditor.API.Serialization.SerializableObject`. Validates that the serializable is a pickup and extracts

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `serializable` | `SerializableObject` | The serialized object to read values from. |

---

### `SpawnObject`

```csharp title="SpawnObject"
public void SpawnObject(
    SchematicData schematic,
    SerializableObject serializable
)
```

Spawns the specified `ThaumielMapEditor.API.Blocks.ServerObject` into the server and adds it to the provided `ThaumielMapEditor.API.Data.SchematicData`'s `ThaumielMapEditor.API.Data.SchematicData.SpawnedServerObjects` list.

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `schematic` | `SchematicData` |  |
| `serializable` | `SerializableObject` |  |

---

