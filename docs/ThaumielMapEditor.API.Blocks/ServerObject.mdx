---
title: ServerObject
sidebar_label: 🔷 ServerObject
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Badge from '@site/src/components/Badge';

# 🔷 ServerObject

<Badge color="blue">class</Badge> <Badge color="secondary">`ThaumielMapEditor.API.Blocks`</Badge>

## Overview

### Properties

| Name | Type | Access | Description |
|------|------|:------:|-------------|
| [`IsStatic`](#isstatic) | `bool` | `get` `set` | Gets or Sets whether or not the ServerObject is static |
| [`MovementSmoothing`](#movementsmoothing) | `byte` | `get` `set` | The ticks between the server sending a update to the client about this object's… |
| [`NetId`](#netid) | `UInt32` | `get` `set` | The NetId of the spawned ServerObject. |
| [`Object`](#object) | `GameObject` | `get` `set` | Gets or sets the associated game object instance. |
| [`ObjectType`](#objecttype) | `ObjectType` | `get` `set` | Gets or sets the type of the object represented by this instance. |
| [`Position`](#position) | `Vector3` | `get` `set` | Gets or sets the Position of the ServerObject |
| [`Rotation`](#rotation) | `Quaternion` | `get` `set` | Gets or sets the Rotation of the ServerObject |
| [`Scale`](#scale) | `Vector3` | `get` `set` | Gets or sets the Scale of the ServerObject |

### Methods

| Signature | Returns | Description |
|-----------|---------|-------------|
| [`DestroyObject(SchematicData)`](#destroyobject) | `void` | Destroys the associated networked object and removes it from the specified schem… |
| [`GetValue(SerializableObject, string)`](#getvalue) | `T` | _No description_ |
| [`SetWorldTransform(SchematicData)`](#setworldtransform) | `void` | Applies the specified schematic's transformation to the current object's world p… |
| [`SpawnObject(SchematicData, SerializableObject)`](#spawnobject) | `void` | Spawns the specified `ThaumielMapEditor.API.Blocks.ServerObject` into the server… |
| [`UpdateObject(SchematicData, bool)`](#updateobject) | `void` | Updates the object's world transform and network state based on the specified sc… |

## Properties

### `IsStatic`

**Type:** `bool`&emsp;**Access:** `get` `set`

Gets or Sets whether or not the ServerObject is static

---

### `MovementSmoothing`

**Type:** `byte`&emsp;**Access:** `get` `set`

The ticks between the server sending a update to the client about this object's position. 0 means no delay, 1 means the server will send an update every tick, 2 means every other tick, and so on.

---

### `NetId`

**Type:** `UInt32`&emsp;**Access:** `get` `set`

The NetId of the spawned ServerObject.

---

### `Object`

**Type:** `GameObject`&emsp;**Access:** `get` `set`

Gets or sets the associated game object instance.

---

### `ObjectType`

**Type:** `ObjectType`&emsp;**Access:** `get` `set`

Gets or sets the type of the object represented by this instance.

---

### `Position`

**Type:** `Vector3`&emsp;**Access:** `get` `set`

Gets or sets the Position of the ServerObject

---

### `Rotation`

**Type:** `Quaternion`&emsp;**Access:** `get` `set`

Gets or sets the Rotation of the ServerObject

---

### `Scale`

**Type:** `Vector3`&emsp;**Access:** `get` `set`

Gets or sets the Scale of the ServerObject

---

## Methods

### `DestroyObject`

```csharp title="DestroyObject"
public void DestroyObject(SchematicData schematic)
```

Destroys the associated networked object and removes it from the specified schematic's collection of spawned
server objects.

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `schematic` | `SchematicData` | The schematic data instance from which the object will be removed. |

---

### `GetValue`

```csharp title="GetValue"
public T GetValue(
    SerializableObject serializable,
    string key
)
```

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `serializable` | `SerializableObject` |  |
| `key` | `string` |  |

#### Returns

`T`

---

### `SetWorldTransform`

```csharp title="SetWorldTransform"
public void SetWorldTransform(SchematicData schematic)
```

Applies the specified schematic's transformation to the current object's world position, rotation, and scale.

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `schematic` | `SchematicData` | The schematic data containing the position, rotation, and scale to apply to the current object. |

---

### `SpawnObject`

```csharp title="SpawnObject"
public void SpawnObject(
    SchematicData schematic,
    SerializableObject serializable
)
```

Spawns the specified `ThaumielMapEditor.API.Blocks.ServerObject` into the server and adds it to the provided `ThaumielMapEditor.API.Data.SchematicData`'s `ThaumielMapEditor.API.Data.SchematicData.SpawnedServerObjects` list.

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `schematic` | `SchematicData` |  |
| `serializable` | `SerializableObject` |  |

---

### `UpdateObject`

```csharp title="UpdateObject"
public void UpdateObject(
    SchematicData schematic,
    bool respawn
)
```

Updates the object's world transform and network state based on the specified schematic data, optionally respawning the object on the network.

:::info Remarks
If the object is not currently spawned, this method will not perform any update. The method also updates the object's network synchronization component if present.
:::

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `schematic` | `SchematicData` | The schematic data used to update the object's position and rotation. |
| `respawn` | `bool` | If `true`, the object is respawned on the network after updating. If `false`, the object is not respawned. |

---

