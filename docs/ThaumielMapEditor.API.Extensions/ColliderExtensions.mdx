---
title: ColliderExtensions
sidebar_label: 📄 ColliderExtensions
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Badge from '@site/src/components/Badge';

# 📄 ColliderExtensions

<Badge color="gray">static class</Badge> <Badge color="secondary">`ThaumielMapEditor.API.Extensions`</Badge>

## Overview

### Methods

| Signature | Returns | Description |
|-----------|---------|-------------|
| [`GetPlayer(Collider)`](#getplayer) | `Player` | Gets a `LabApi.Features.Wrappers.Player` from a `UnityEngine.Collider` |
| [`TryGetPlayer(Collider, Player&)`](#trygetplayer) | `bool` | Tries to get a `LabApi.Features.Wrappers.Player` from a `UnityEngine.Collider` u… |

## Methods

### `GetPlayer`

```csharp title="GetPlayer"
public Player GetPlayer(Collider collider)
```

Gets a `LabApi.Features.Wrappers.Player` from a `UnityEngine.Collider`

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `collider` | `Collider` | The `UnityEngine.Collider` to check for a `LabApi.Features.Wrappers.Player` |

#### Returns

`Player` — `LabApi.Features.Wrappers.Player` if not `null` else returns `null`

---

### `TryGetPlayer`

```csharp title="TryGetPlayer"
public bool TryGetPlayer(
    Collider collider,
    Player& player
)
```

Tries to get a `LabApi.Features.Wrappers.Player` from a `UnityEngine.Collider` using `ThaumielMapEditor.API.Extensions.ColliderExtensions.GetPlayer(UnityEngine.Collider)`

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `collider` | `Collider` | The `UnityEngine.Collider` to check for a `LabApi.Features.Wrappers.Player` |
| `player` | `Player&` | The `LabApi.Features.Wrappers.Player` if found. |

#### Returns

`bool` — `true` if a `LabApi.Features.Wrappers.Player` was found else returns `false` when no `LabApi.Features.Wrappers.Player` is found.

---

