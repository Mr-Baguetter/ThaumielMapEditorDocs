---
title: FileManager
sidebar_label: 🔷 FileManager
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Badge from '@site/src/components/Badge';

# 🔷 FileManager

<Badge color="blue">class</Badge> <Badge color="secondary">`ThaumielMapEditor.API.Helpers`</Badge>

## Overview

### Methods

| Signature | Returns | Description |
|-----------|---------|-------------|
| [`CreateDefaultFile(string, string)`](#createdefaultfile) | `void` | _No description_ |
| [`CreateDefaultFiles(string, List<ValueTuple<string, T>>)`](#createdefaultfiles) | `void` | _No description_ |
| [`Dir()`](#dir) | `string` | Gets the Thaumiel directory |
| [`Dir(string[])`](#dir) | `string` | Gets the Thaumiel directory plus the inputed directories |
| [`GetFilesInDirectory(string, string)`](#getfilesindirectory) | `string[]` | Gets all the file paths in the Thaumiel directory combined with the specified di… |
| [`ParseYamlFiles(string, Action<T>)`](#parseyamlfiles) | `void` | _No description_ |
| [`TryCreateDirectory(string)`](#trycreatedirectory) | `void` | Tries to create a directory with the inputted name |
| [`TryCreateDirectory(string[])`](#trycreatedirectory) | `void` | Tries to create a directory at the path |

## Methods

### `CreateDefaultFile`

```csharp title="CreateDefaultFile"
public void CreateDefaultFile(
    string name,
    string fileName
)
```

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `name` | `string` |  |
| `fileName` | `string` |  |

---

### `CreateDefaultFiles`

```csharp title="CreateDefaultFiles"
public void CreateDefaultFiles(
    string name,
    List<ValueTuple<string, T>> items
)
```

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `name` | `string` |  |
| `items` | `List<ValueTuple<string, T>>` |  |

---

### `Dir`

```csharp title="Dir"
public string Dir()
```

Gets the Thaumiel directory

#### Returns

`string` — Directory path to the Thaumiel directory

---

### `Dir`

```csharp title="Dir"
public string Dir(string[] path)
```

Gets the Thaumiel directory plus the inputed directories

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `path` | `string[]` | The path of directories |

#### Returns

`string` — Directory path to the Thaumiel directory plus the inputed directories

---

### `GetFilesInDirectory`

```csharp title="GetFilesInDirectory"
public string[] GetFilesInDirectory(
    string name,
    string filter
)
```

Gets all the file paths in the Thaumiel directory combined with the specified directory path.

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `name` | `string` | The directory path relative to the Thaumiel directory. |
| `filter` | `string` | The search pattern to filter files by. Defaults to `*` which returns all files. |

#### Returns

`string[]` — An array of file paths matching the `filter` in the resolved directory.

---

### `ParseYamlFiles`

```csharp title="ParseYamlFiles"
public void ParseYamlFiles(
    string name,
    Action<T> onParsed
)
```

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `name` | `string` |  |
| `onParsed` | `Action<T>` |  |

---

### `TryCreateDirectory`

```csharp title="TryCreateDirectory"
public void TryCreateDirectory(string name)
```

Tries to create a directory with the inputted name

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `name` | `string` | The name of the directory |

---

### `TryCreateDirectory`

```csharp title="TryCreateDirectory"
public void TryCreateDirectory(string[] path)
```

Tries to create a directory at the path

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `path` | `string[]` | The directory path to make |

---

