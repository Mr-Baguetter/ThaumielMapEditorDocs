---
title: AssetBundleControllerLoader
sidebar_label: 📄 AssetBundleControllerLoader
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Badge from '@site/src/components/Badge';

# 📄 AssetBundleControllerLoader

<Badge color="gray">static class</Badge> <Badge color="secondary">`ThaumielMapEditor.API.Animation`</Badge>

## Overview

### Methods

| Signature | Returns | Description |
|-----------|---------|-------------|
| [`LoadController(string, string)`](#loadcontroller) | `RuntimeAnimatorController` | Loads a RuntimeAnimatorController from a bundle file sitting next to the schemat… |

## Methods

### `LoadController`

```csharp title="LoadController"
public RuntimeAnimatorController LoadController(
    string schematicFileName,
    string controllerName
)
```

Loads a RuntimeAnimatorController from a bundle file sitting next to the schematic. The bundle file should be named after the controller e.g. "DoorController".

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `schematicFileName` | `string` |  |
| `controllerName` | `string` |  |

#### Returns

`RuntimeAnimatorController`

---

